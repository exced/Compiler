int a;

int main(){

	// OPREL
	bool le = (0 < 1);
	
	bool leq = 3 <= 6;
	
	bool eq = (1 == 0);
	
	bool ge = (3 > 2);
	
	bool geq = 4 >= (3 + 1);
	
	// OPADD
	int plus = 1 + 2;
	
	// OPMUL
	int mult = 3 * 12;
	
	// OPUN
	int moins = -5;
	
	// OPUN
	bool not = !true;
	
	// OPUN
	int plus2 = +4;
	

    return 0;
}

